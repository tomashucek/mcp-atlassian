# =============================================================================
# MCP Atlassian ComAp Integration - Environment Configuration
# =============================================================================
# This file shows the environment variables needed for the MCP Atlassian server
# when running in production. The DXT version will prompt for these values.

# =============================================================================
# JIRA Configuration
# =============================================================================
JIRA_URL=https://your-company.atlassian.net
JIRA_USERNAME=your.email@company.com
JIRA_API_TOKEN=your_jira_api_token_or_pat

# Optional: Filter JIRA projects (comma-separated project keys)
# JIRA_PROJECTS_FILTER=PROJ,DEV,SUPPORT

# =============================================================================
# Confluence Configuration  
# =============================================================================
CONFLUENCE_URL=https://your-company.atlassian.net/wiki
CONFLUENCE_USERNAME=your.email@company.com
CONFLUENCE_API_TOKEN=your_confluence_api_token_or_pat

# Optional: Filter Confluence spaces (comma-separated space keys)
# CONFLUENCE_SPACES_FILTER=DEV,TEAM,DOC

# =============================================================================
# ComAp Enterprise Configuration (FIXED - DO NOT MODIFY)
# =============================================================================
# These headers are required for ComAp firewall access and are hardcoded in the DXT
# JIRA_CUSTOM_HEADERS=User-Agent=Comup Integrations
# CONFLUENCE_CUSTOM_HEADERS=User-Agent=Comup Integrations

# =============================================================================
# Optional Configuration
# =============================================================================
# Read-only mode (disable write operations)
# READ_ONLY_MODE=false

# SSL Verification (for Server/Data Center with self-signed certs)
# JIRA_SSL_VERIFY=true
# CONFLUENCE_SSL_VERIFY=true

# Proxy Configuration (if needed)
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,.company.com

# Debug logging
# MCP_VERBOSE=false
# MCP_VERY_VERBOSE=false
# MCP_LOGGING_STDOUT=true